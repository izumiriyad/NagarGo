# NagarGo Backend

Install from this directory with `npm install`, then run `npm run build:api` and `npm run start:api`.
Set `PORT`, `API_BASE_URL`, MongoDB, Telegram, Google/Cloudinary, and other values from the included environment file.
