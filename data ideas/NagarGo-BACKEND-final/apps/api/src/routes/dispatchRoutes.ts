import {Router} from "express"; import {requireAuth,requireRole} from "../middleware/auth"; import {dispatchOrder,advanceOrder,riderRejectAssignment} from "../controllers/dispatchController"; const r=Router(); r.post("/orders/:id/dispatch",requireAuth,requireRole("ADMIN"),dispatchOrder); r.post("/orders/:id/advance",requireAuth,requireRole("RIDER"),advanceOrder); r.post("/orders/:id/reject-assignment",requireAuth,requireRole("RIDER"),riderRejectAssignment); export default r;
