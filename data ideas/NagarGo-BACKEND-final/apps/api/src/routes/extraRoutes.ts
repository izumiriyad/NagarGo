import { Router } from "express"; import { requireAuth,requireRole } from "../middleware/auth"; import { savedAddresses,deleteSavedAddress,rateOrder,disputeOrder,myDisputes,referral } from "../controllers/extraController"; const r=Router(); r.get("/addresses",requireAuth,requireRole("CUSTOMER"),savedAddresses); r.post("/addresses",requireAuth,requireRole("CUSTOMER"),savedAddresses); r.delete("/addresses/:id",requireAuth,requireRole("CUSTOMER"),deleteSavedAddress); r.post("/orders/:id/rating",requireAuth,requireRole("CUSTOMER"),rateOrder); r.post("/orders/:id/dispute",requireAuth,requireRole("CUSTOMER"),disputeOrder); r.get("/disputes",requireAuth,requireRole("CUSTOMER"),myDisputes); r.get("/referral",requireAuth,requireRole("CUSTOMER"),referral); export default r;
